/**
 * @see https://developer.apple.com/library/ios/navigation/
 */

#   CGGeometry
    // Data Types
    .CGRect {origin:CGPoint, size:CGSize, init(origin, size)}
    .CGSize {width, height, init(width, height)}
    .CGPoint{x, y, init(x, y)}
    .CGVector {dx, dy, init(dx, dy)}
