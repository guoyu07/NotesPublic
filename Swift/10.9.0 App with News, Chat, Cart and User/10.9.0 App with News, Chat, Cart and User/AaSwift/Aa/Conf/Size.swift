import UIKit
extension Conf {
    struct Size {
        struct TableView {
            static let cellHeight: CGFloat = 28
            static let cellMargins = UIEdgeInsets(top: 10, left: 10, bottom: 10, right: 10)
            static let detailViewImageSize = CGSize(width: CGFloat(16), height: CGFloat(16))
        }
    }
}