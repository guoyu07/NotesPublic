|! mount share directory !|
|+ install addition.iso +|
`Devices` -> `Insert Guest Additions CD image ...` 
|+ map share directory +|
Share Directory path: E:\test\share
Share Directory name: ShareName
auto mount + fixed
|+ mount +|
sh$ yum install gcc gcc-c++ kernel-devel-$(uname -r)
sh$ sudo /run/media/$user/VBOXADDITIONS_xxx/VBoxLinuxAdditions.run
sh$ mkdir /share_dir/


/* runtime: sh# mount -t vboxsf ShareName /share_dir/ */
/*
    sh# vim /etc/fstab
    |[
    ShareName /share_dir vboxsf defaults 0 0
    ]|
*/

/* mount automatically upon each boot */
sh$ sudo mount -t vboxsf -o rw,uid=1000,gid=1000 ShareName /share_dir/